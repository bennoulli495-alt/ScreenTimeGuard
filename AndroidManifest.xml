<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Core permissions -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.ScreenTimeGuard"
        android:supportsRtl="true">

        <!-- Main configuration screen. Deliberately visible in the launcher —
             this app is meant to be known to and manageable by anyone using
             the device, consistent with transparent family screen-time tools. -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.AppSelectionActivity"
            android:exported="false"
            android:label="Select Apps to Restrict" />

        <activity
            android:name=".ui.PasswordActivity"
            android:exported="false"
            android:label="Admin Password"
            android:parentActivityName=".ui.SettingsActivity" />

        <activity
            android:name=".ui.RecoveryQuestionActivity"
            android:exported="false"
            android:label="Recover Password"
            android:parentActivityName=".ui.PasswordActivity" />

        <activity
            android:name=".ui.PermissionsActivity"
            android:exported="false"
            android:label="Required Permissions"
            android:parentActivityName=".ui.SettingsActivity" />

        <activity
            android:name=".ui.ScheduleListActivity"
            android:exported="false"
            android:label="Schedules"
            android:parentActivityName=".ui.SettingsActivity" />

        <activity
            android:name=".ui.ScheduleEditActivity"
            android:exported="false"
            android:label="Edit Schedule"
            android:parentActivityName=".ui.ScheduleListActivity" />

        <!-- Foreground service that watches the current foreground app -->
        <service
            android:name=".service.AppMonitorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Parental/family screen-time enforcement" />
        </service>

        <!-- Restarts the monitor service after a reboot -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Fires once daily at midnight to clear each schedule's
             "unlocked for today" state. This is the only alarm the app
             ever registers, regardless of how many schedules exist. -->
        <receiver
            android:name=".receiver.DailyResetReceiver"
            android:exported="false" />

        <!-- Device Admin receiver: adds a confirmation step before the app
             can be uninstalled or force-stopped. This is a standard Android
             mechanism (used by e.g. Family Link) and can always be turned
             off by the device user from Settings > Security > Device admin apps. -->
        <receiver
            android:name=".receiver.MyDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
